- if @studio_brand.class_templates.any?
  %ul.list-group
    - sorted_records(@studio_brand.class_templates, :name).each do |class_template|
      %li.list-group-item
        = link_to class_template.name, edit_admin_class_template_path(class_template)
