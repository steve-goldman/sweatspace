= render partial: "shared/errors_for", locals: { object: @studio }
%p
  = link_to admin_studios_path, class: "btn btn-default" do
    %span
      %i.glyphicon.glyphicon-arrow-left
.panel.panel-default
  .panel-heading
    .panel-title< Studio Details
  .panel-body
    = form_for @studio, url: { action: :update} do |f|
      = render partial: "form_fields", locals: { f: f }
.panel.panel-default
  .panel-heading
    .panel-title< Associated Studio Brands
  = render partial: "studio_brand_list"
  .panel-footer
    = form_for :studio_brand_studio, url: admin_studio_brand_studios_path, method: :post do |f|
      = render partial: "studio_brand_form_fields", locals: { f: f }
