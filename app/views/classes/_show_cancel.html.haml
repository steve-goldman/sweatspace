.panel.panel-default
  .panel-heading
    %h3.panel-title< Cancel Class
  .panel-body
    %p< Click here to indicate on your schedule that the class has been canceled.
    = form_for :class_change, url: cancel_class_change_path(@clazz) do |f|
      = f.submit "Cancel Class", class: "btn btn-danger", data: { confirm: "Are you sure you want to cancel the class?" }
