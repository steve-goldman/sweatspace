%h3= @instructor_profile.full_name
= image_tag @instructor_profile.profile_picture_url
- if @instructor_profile.classes.any?
  %h4< Classes
  %table.table
    %tbody
      - @instructor_profile.classes.each do |clazz|
        %tr
          %td= clazz.display_time
          %td= clazz.name
          %td= clazz.display_clazz_type
          %td= link_to clazz.display_studio, clazz.studio_url
          %td= clazz.display_duration
          %td= clazz.description
- else
  %h4< No classes
