- if flash.any?
  .flash
    .row
      .col-xs-offset-1.col-xs-10
        - flash.each do |key, value|
          %div{ class: "shadow alert alert-#{flash_key(key)}" }= value
  :javascript
    HideFlash();
