%nav#my-menu
  %ul
    - if user_signed_in?
      = render partial: "layouts/navigation/signed_in_user"
      - if current_user.admin?
        = render partial: "layouts/navigation/admin"
    - else
      = render partial: "layouts/navigation/signed_out_user"
