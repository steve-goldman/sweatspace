= render partial: "shared/errors_for", locals: { object: @recurring_class }
= link_to recurring_classes_path(@recurring_class), class: "btn btn-default" do
  %span
    %i.glyphicon.glyphicon-arrow-left
%h3< Create a recurring class
= form_for @recurring_class, url: { action: :create } do |f|
  = render partial: "form_fields", locals: { f: f }
